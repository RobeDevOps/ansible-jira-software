---
- name: Install package rpm postgresql
  yum:
    name: 
    state: present
    update_cache: yes
  with_items:
    - "http://download.postgresql.org/pub/repos/yum/{{postgres_version}}/{{postgres_platform}}/{{platform_arch}}/{{rpm_package}}"
    - "{{postgres_package}}"